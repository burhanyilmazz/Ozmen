$ {
    const classes = ["a-title-icon"];
    const attrs = input.attrs || {};

    if (input.class) {
        classes.push(input.class)
    }
}
<div class=classes ...attrs>
    <i class=`icon icon-${input.icon}`></i>
    <span>${input.title}</span>
</div>